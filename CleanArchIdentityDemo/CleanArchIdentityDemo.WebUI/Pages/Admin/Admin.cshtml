@page
@model CleanArchIdentityDemo.WebUI.Pages.Admin.AdminModel
@{
    ViewData["Title"] = "Administración de Usuarios";
}

<div class="toast-container position-fixed top-0 end-0 p-3" style="z-index: 11; margin-top: 25px;">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="toast align-items-center text-white bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true" id="successToast">
            <div class="d-flex">
                <div class="toast-body">
                    <i class="fas fa-check-circle me-2"></i>
                    @TempData["SuccessMessage"]
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="toast align-items-center text-white bg-danger border-0" role="alert" aria-live="assertive" aria-atomic="true" id="errorToast">
            <div class="d-flex">
                <div class="toast-body">
                    <i class="fas fa-exclamation-circle me-2"></i>
                    @TempData["ErrorMessage"]
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
        </div>
    }

    @if (TempData["WarningMessage"] != null)
    {
        <div class="toast align-items-center text-white bg-warning border-0" role="alert" aria-live="assertive" aria-atomic="true" id="warningToast">
            <div class="d-flex">
                <div class="toast-body">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    @TempData["WarningMessage"]
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
        </div>
    }

    @if (TempData["InfoMessage"] != null)
    {
        <div class="toast align-items-center text-white bg-info border-0" role="alert" aria-live="assertive" aria-atomic="true" id="infoToast">
            <div class="d-flex">
                <div class="toast-body">
                    <i class="fas fa-info-circle me-2"></i>
                    @TempData["InfoMessage"]
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
        </div>
    }
</div>

<h2>Administración de Usuarios</h2>

<nav>
    <div class="nav nav-tabs" id="nav-tab" role="tablist">
        <button class="nav-link" id="nav-Usuarios" data-bs-toggle="tab" data-bs-target="#nav-Usuarios-tab" type="button" role="tab" aria-controls="nav-Usuarios-tab" aria-selected="false">
            Usuarios
        </button>
        <button class="nav-link" id="nav-RespaldosAutomaticosBtn" data-bs-toggle="tab" data-bs-target="#nav-RespaldosAutomaticos-tab"
                type="button" role="tab" aria-controls="nav-RespaldosAutomaticos" aria-selected="false">
            Respaldos automáticos
        </button>
        <button class="nav-link" id="nav-RespaldosManualesBtn" data-bs-toggle="tab" data-bs-target="#nav-RespaldosManuales-tab" type="button" role="tab" aria-controls="nav-RespaldosManuales-tab" aria-selected="false" onclick="cargarBackupsManuales()">
            Respaldos manuales
        </button>
    </div>
</nav>


<div class="tab-content" id="nav-tabContent">

    <div class="tab-pane fade show active" id="nav-Usuarios-tab" role="tabpanel" aria-labelledby="nav-Usuarios">
        <br />
        <div class="row justify-content-between">
            <div class="col-4">
                Lista de usuarios
            </div>
            <div class="col-4">
                <!-- Botón para abrir modal Crear Usuario -->
                <button class="btn btn-primary mb-3" data-bs-toggle="modal" data-bs-target="#createUserModal">
                    Crear Usuario
                </button>
            </div>
        </div>
        <div>
            <!-- Tabla de Usuarios -->
            <table class="table table-striped align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Email</th>
                        <th>Nombre Completo</th>
                        <th>Rol</th>
                        <th>Estado</th>
                        <th class="align-content-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Users != null && Model.Users.Any())
                    {
                        @foreach (var user in Model.Users)
                        {
                            <tr>
                                <td>@user.Email</td>
                                <td>@user.NombreCompleto</td>
                                <td>@user.Role</td>
                                <td>
                                    @if(!user.Activo)
                                    {
                                        <small class="badge bg-danger">Desactivado</small>
                                    }
                                    else
                                    {
                                        <small class="badge bg-success">Activo</small>
                                    }
                                </td>
                                <td>
                                    <div class="container">
                                        <div class="row">
                                            <div class="col">
                                                <!-- Contenido para columna accion 1 -->
                                                <button class="btn btn-sm btn-warning"
                                                        data-bs-toggle="modal"
                                                        data-bs-target="#editUserModal"
                                                        data-id="@user.Id"
                                                        data-email="@user.Email"
                                                        data-nombre="@user.NombreCompleto"
                                                        data-activo="@user.Activo"
                                                        data-rol="@user.Role">
                                                    <i class="fas fa-edit"></i> Editar
                                                </button>
                                            </div>
                                            @if (user.Activo)
                                            {
                                                <div class="col">
                                                    <!-- Contenido para columna accion 2 -->
                                                    <form method="post" asp-page-handler="Delete">
                                                        <input type="hidden" name="IdUsuario" value="@user.Id" />
                                                        <button class="btn btn-sm btn-danger">
                                                            <i class="fas fa-ban"></i> Desactivar
                                                        </button>
                                                    </form>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="col">
                                                    <!-- Contenido para columna accion 2 -->
                                                    <form method="post" asp-page-handler="Activate">
                                                        <input type="hidden" name="IdUsuario" value="@user.Id" />
                                                        <button class="btn btn-sm btn-info">
                                                            <i class="fa-solid fa-circle-check"></i> Activar
                                                        </button>
                                                    </form>
                                                </div>
                                            }
                                            
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4" class="text-center">No hay usuarios disponibles.</td>
                        </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>

    <div class="tab-pane fade" id="nav-RespaldosAutomaticos-tab" role="tabpanel" aria-labelledby="nav-RespaldosAutomaticos">
        <br />
        <div class="container-fluid mt-4">
            <!-- Información PITR -->
            <div class="row">
                <div>
                    <div class="card">
                        <div class="card-header bg-info text-white">
                            <h5>Backups Automáticos (PITR): Copias de seguridad automáticas para permitir la restauración a un punto en el tiempo (Point In Time Restore).</h5>
                        </div>
                        <div class="card-body">
                            <p>
                                <strong>Retención:</strong> 7 días (puede ser configurable hasta 35 días por medio de Azure Portal)
                            </p>
                            
                            <p class="text-muted mb-0">
                                <p>
                                    En esta pestaña solo se muestran los registros de estos puntos de restauración.
                                </p>
                                <p>
                                    Azure SQL Database crea:
                                </p>
                                    <ul>
                                       <li>
                                           Copias de seguridad automáticas para permitir la restauración a un punto en el tiempo (PITR - Point In Time Restore).
                                       </li>
                                        <li>
                                            Copias de seguridad completas cada semana.
                                        </li>
                                        <li>
                                            Copias de seguridad diferenciales se realizan cada 12h o 24h (es configurable).
                                        </li>
                                        <li>
                                            Las copias de seguridad del registro de transacciones se realizan aproximadamente cada 10 minutos.
                                        </li>
                                    </ul>
                                <small>
                                    Los puntos de resturación obtenidos son resultado de las copias de seguridad o backups automáticos.

                                    La única acción a tomar para estos puntos de restauración es restaurarlos únicamente, y para hacerlo solamente es posible ingresando al Azure Portal donde puede realizar más configuraciones de ser necesario.
                                    Puede tener mas información en: https://learn.microsoft.com/en-us/azure/azure-sql/database/automated-backups-overview?view=azuresql
                                </small>
                        </div>
                    </div>
                </div>
            </div>

            <!--- Tabla de backups automaticos --->
            <div class="position-relative">
                <div class="table-responsive" id="tablaBackupsAutomaticosContainer">
                    <partial name="_TablaBackupsPITR" model="Model.PuntosRestauracion" />
                </div>

                <div id="loadingSpinnerAutomaticos" class="text-center py-5" style="display: none;">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Cargando...</span>
                    </div>
                    <p class="mt-2 text-muted">Cargando datos de backups automáticos...</p>
                </div>
            </div>

        </div>
    </div>


    <div class="tab-pane fade" id="nav-RespaldosManuales-tab" role="tabpanel" aria-labelledby="nav-RespaldosManuales-tab">
        <br />
        <div class="row justify-content-between">
            <div class="col-4">
                
                        Backups manuales
            </div>
            <div class="col-4">
                <!-- Botón para abrir modal Crear Usuario -->
                <button class="btn btn-primary mb-3" data-bs-toggle="modal" data-bs-target="#crearBackupModal">
                    Crear backup
                </button>
            </div>
        </div>

        <!--- Tabla de backups manuales --->
        <div class="position-relative">
            <div class="table-responsive" id="tablaBackupsManualesContainer">
                <partial name="_TablaRespaldosManuales" model="Model.BackupsManuales" />
            </div>

            <div id="loadingSpinnerManuales" class="text-center py-5" style="display: none;">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">Cargando...</span>
                </div>
                <p class="mt-2 text-muted">Cargando datos de backups manuales...</p>
            </div>
        </div>

        <!-- Backups Manuales -->
        @*<div class="card">
            <div class="card-header">
                <h5>Backups Manuales Disponibles</h5>
            </div>
           <div class="card-body">
                @if (!Model.BackupsManuales.Any())
                {
                    <p class="text-muted">No hay backups manuales creados</p>
                }
                else
                {
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Archivo</th>
                                <th>Fecha</th>
                                <th>Tamaño</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var backup in Model.BackupsManuales)
                            {
                                <tr>
                                    <td>@backup.NombreArchivo</td>
                                    <td>@backup.FechaCreacion.ToString("dd/MM/yyyy HH:mm")</td>
                                    <td>@backup.TamanoFormateado</td>
                                    <td>
                                        <a href="@backup.UrlDescarga"
                                           class="btn btn-sm btn-primary"
                                           target="_blank">
                                            <i class="fas fa-download"></i> Descargar
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>*@      
        </div>
</div>



<!-- Modal Crear Usuario -->
<div class="modal fade" id="createUserModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" asp-page-handler="Create">
                <div class="modal-header">
                    <h5 class="modal-title">Crear Usuario</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label>Email</label>
                        <input asp-for="NewUser.Email" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label>Nombre Completo</label>
                        <input asp-for="NewUser.NombreCompleto" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label>Rol</label>
                        <select asp-for="NewUser.Role" class="form-control">
                            @foreach (var rol in Model.Roles)
                            {
                                <option value="@rol.NombreRol">@rol.NombreRol</option>
                            }
                        </select>
                    </div>
                    <div class="mb-3">
                        <label>Contraseña</label>
                        <input asp-for="NewUser.Password" type="password" class="form-control" />
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-success">Crear</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Modal Editar Usuario -->
<div class="modal fade" id="editUserModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" asp-page-handler="Edit">
                <div class="modal-header">
                    <h5 class="modal-title">Editar Usuario</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" asp-for="EditedUser.Id" />
                    <input type="hidden" asp-for="EditedUser.Activo" />
                    <div class="mb-3">
                        <label>Email</label>
                        <input asp-for="EditedUser.Email" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label>Nombre Completo</label>
                        <input asp-for="EditedUser.NombreCompleto" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label>Rol</label>
                        <select asp-for="EditedUser.Role" class="form-control">
                            @foreach (var rol in Model.Roles)
                            {
                                <option value="@rol.NombreRol">@rol.NombreRol</option>
                            }
                        </select>
                    </div>
                    <div class="mb-3">
                        <label>Nueva contraseña</label>
                        <input asp-for="EditedUser.Password" type="password" class="form-control" placeholder="De no dar un contraseña se mantendra la actual" />
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-success">Guardar</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Crear Backup Manual -->
<div class="modal fade" id="crearBackupModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Crear backup</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form method="post" asp-page-handler="CrearBackupManual">
                    <div class="mb-3">
                        <label class="form-label">Nombre del Backup</label>
                        <input type="text"
                               class="form-control"
                               asp-for="NombreBackup"
                               placeholder="backup_pre_actualizacion"
                               required>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-primary"> <i class="fas fa-save"></i> Crear Backup </button>
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Mostrar todos los toasts que existan
            var toastElList = [].slice.call(document.querySelectorAll('.toast'));
            var toastList = toastElList.map(function(toastEl) {
                return new bootstrap.Toast(toastEl, {
                    autohide: true,
                    delay: 3000  // 3 segundos
                });
            });

            // Activar cada toast
            toastList.forEach(toast => toast.show());

        //Event listener para el boton que muestra la pestanna de respaldos automaticos
         const respaldosButton = document.getElementById('nav-RespaldosAutomaticosBtn');

        if (respaldosButton) {
            respaldosButton.addEventListener('click', cargarBackupsAutomaticos);
        }

        // //Event listener para el boton que muestra la pestanna de respaldos manueales
        // const respaldosManualesButton = document.getElementById('nav-RespaldosManualesBtn');

         // if (respaldosManualesButton) {
         //     respaldosManualesButton.addEventListener('click', cargarBackupsManuales);
         // }


        });
        // Llenar modal Editar con datos del botón pulsado
        var editUserModal = document.getElementById('editUserModal');
        editUserModal.addEventListener('show.bs.modal', function (event) {
            var button = event.relatedTarget;
            var id = button.getAttribute('data-id');
            var email = button.getAttribute('data-email');
            var nombre = button.getAttribute('data-nombre');
            var rol = button.getAttribute('data-rol');
            var activo = button.getAttribute('data-activo');

            document.getElementById('EditedUser_Id').value = id;
            document.getElementById('EditedUser_Email').value = email;
            document.getElementById('EditedUser_NombreCompleto').value = nombre;
            document.getElementById('EditedUser_Role').value = rol;
            document.getElementById('EditedUser_Activo').value = activo;
        });
    </script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var tabActiva = '@TempData["TabActiva"]';
            if(tabActiva) {
                var triggerEl = document.querySelector(`#nav-${tabActiva}`);
                if(triggerEl){
                    var tab = new bootstrap.Tab(triggerEl);
                    tab.show();
                }
            }
        });
    </script>
    <script>
        async function cargarBackupsAutomaticos() {
            const tabla = document.getElementById('tablaBackupsAutomaticosContainer');
            const spinner = document.getElementById('loadingSpinnerAutomaticos'); // ⚠️ ID actualizado

            try {
                tabla.style.display = 'none';
                spinner.style.display = 'block';

                const response = await fetch('?handler=CargarBackupsAutomaticos', {
                    method: 'GET',
                    headers: { 'X-Requested-With': 'XMLHttpRequest' }
                });

                if (!response.ok) throw new Error('Error en la petición');

                tabla.innerHTML = await response.text();

            } catch (error) {
                console.error('Error:', error);
                tabla.innerHTML = '<div class="alert alert-danger">Error al obtener los datos de backups automáticos</div>';
            } finally {
                spinner.style.display = 'none';
                tabla.style.display = 'block';
            }
        }

        async function cargarBackupsManuales() {
            const tabla = document.getElementById('tablaBackupsManualesContainer');
            const spinner = document.getElementById('loadingSpinnerManuales'); // ⚠️ ID actualizado

            try {
                tabla.style.display = 'none';
                spinner.style.display = 'block';

                const response = await fetch('?handler=CargarBackupsManuales', {
                    method: 'GET',
                    headers: { 'X-Requested-With': 'XMLHttpRequest' }
                });

                if (!response.ok) throw new Error('Error en la petición');

                tabla.innerHTML = await response.text();

            } catch (error) {
                console.error('Error:', error);
                tabla.innerHTML = '<div class="alert alert-danger">Error al obtener los datos de backups manuales</div>';
            } finally {
                spinner.style.display = 'none';
                tabla.style.display = 'block';
            }
        }
    </script>
    
}
